{
  "name": "react-pdv-example-cra",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-pdv": "^0.0.22",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "format": "prettier --single-quote --no-semi **/*.js",
    "predeploy": "yarn build",
    "deploy": "now-alias ./build --name=react-pdv-example-cra --alias=react-pdv-example-cra.now.sh",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "now-alias": "^1.1.0",
    "prettier": "^1.13.5"
  }
}
